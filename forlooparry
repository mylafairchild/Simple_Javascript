// For loop with an array

var movies = ["Fight Club", "District 13", "Kontroll", "Donnie Darko", "Prometheus"];

for (var i = 0; i < movies.length; i+=1) {
    console.log("I would like to see " + movies[i]);
}
